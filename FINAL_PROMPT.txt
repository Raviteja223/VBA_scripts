You are acting as a Senior Principal Data Engineer, Python Engineer, Airflow/Astronomer Architect, Excel Reverse-Engineering Specialist, and QA Engineer.

You have TWO primary inputs attached:

1. remarketing_airflow_automation_final.zip
   - This is the current Python/Airflow automation project.
   - It contains a complete runnable engineering framework.
   - Some business calculations are provisional and MUST NOT be assumed to be correct.

2. WORKBOOK_LOGIC_SPEC.md
   - This was produced by deeply analyzing four Excel metadata extraction files:
     - workbook structure
     - formula patterns
     - named ranges
     - PivotTable definitions
   - This document is the authoritative source for confirmed workbook behavior.

============================================================
CRITICAL OBJECTIVE
============================================================

Update the existing ZIP project so that it reproduces the existing Excel-based Remarketing / Repossession process as accurately as the evidence in WORKBOOK_LOGIC_SPEC.md allows.

The target flow is:

Excel/XLSX
    ↓
GCS
    ↓
Airflow / Astronomer
    ↓
Workbook validation
    ↓
Read source sheets
    ↓
Standardization
    ↓
Mappings/lookups
    ↓
Confirmed Excel calculations
    ↓
Monthly reporting
    ↓
YTD reporting
    ↓
Pivot/report reconstruction
    ↓
XLSX output
    ↓
Reconciliation
    ↓
Archive / notification / audit

The most important acceptance criterion is:

AUTOMATED OUTPUT MUST RECONCILE WITH THE EXISTING MANUAL EXCEL PROCESS FOR THE SAME INPUT DATA.

============================================================
SECURITY / DATA-HANDLING RULES
============================================================

All attached files and project information must remain inside this approved environment.

DO NOT:

- expose confidential source data
- reproduce VINs
- reproduce contract numbers
- reproduce customer names
- reproduce vehicle-level transactional rows
- expose proprietary sample values unnecessarily
- create external uploads
- call external services with workbook contents
- invent business rules to make tests pass

You may use synthetic data for unit tests.

All test fixtures must use fabricated values only.

============================================================
IMPORTANT SOURCE-OF-TRUTH RULE
============================================================

WORKBOOK_LOGIC_SPEC.md is authoritative for:

- confirmed formulas
- formula dependencies
- named ranges
- lookup behavior
- PivotTable definitions
- monthly reporting rules
- YTD logic
- inventory / return / sale / aging rules
- report definitions
- edge-case behavior where confirmed
- implementation mapping
- reconciliation requirements

However:

If WORKBOOK_LOGIC_SPEC.md marks something as:

REQUIRES_ANALYST_VALIDATION

or:

UNCONFIRMED

or:

LOW CONFIDENCE

DO NOT convert it into a hard-coded business rule.

Instead:

1. isolate the uncertainty
2. leave the behavior configurable where possible
3. document it
4. add it to OPEN_QUESTIONS.md
5. do not pretend the implementation is business-approved

============================================================
VERY IMPORTANT
============================================================

The current ZIP project contains provisional logic.

DO NOT assume that existing calculations.py formulas are correct.

For every existing business calculation:

CURRENT CODE
    vs
WORKBOOK_LOGIC_SPEC.md

must be compared.

The specification wins when there is confirmed evidence.

Do NOT preserve an incorrect function merely because it already exists.

At the same time, preserve public function names/signatures where practical so that the DAGs and runner do not break unnecessarily.

============================================================
PHASE 1 — EXTRACT AND INSPECT THE ENTIRE PROJECT
============================================================

First extract:

remarketing_airflow_automation_final.zip

Inspect the ENTIRE project before modifying anything.

Inventory:

- config/
- dags/
- include/remarketing/
- scripts/
- tests/
- README.md
- BUSINESS_RULES.md
- OPEN_QUESTIONS.md
- requirements.txt
- pytest.ini

Produce a short internal assessment:

PROJECT INVENTORY

| Area | Files | Purpose | Current State |

Do not write code yet.

============================================================
PHASE 2 — READ THE COMPLETE SPECIFICATION
============================================================

Read WORKBOOK_LOGIC_SPEC.md from beginning to end.

Do not skim it.

Specifically extract:

- confirmed business rules
- high-confidence rules
- unconfirmed rules
- formula rule IDs
- source columns
- derived columns
- dependencies
- lookup rules
- monthly rules
- YTD rules
- PivotTables
- report definitions
- null handling
- historical correction behavior
- reconciliation requirements
- open analyst questions

Create an internal matrix:

| Spec Rule ID | Current Python Implementation | Match? | Required Change |

Values for Match:

EXACT_MATCH
PARTIAL_MATCH
NO_MATCH
NOT_IMPLEMENTED
UNCONFIRMED

============================================================
PHASE 3 — PERFORM A GAP ANALYSIS
============================================================

Compare the existing project to the specification.

Create:

# IMPLEMENTATION GAP ANALYSIS

For every confirmed specification rule identify:

- already implemented correctly
- implemented incorrectly
- partially implemented
- missing entirely
- blocked by business ambiguity

Do the same for:

1. Workbook validation
2. Column normalization
3. Data standardization
4. Lookups
5. Formula calculations
6. Inventory calculations
7. Return calculations
8. Sale calculations
9. Aging calculations
10. Monthly reporting
11. YTD reporting
12. Pivot reporting
13. Final report generation
14. Reconciliation
15. Historical correction/rebuild behavior
16. Airflow orchestration
17. GCS interaction
18. Audit logging
19. Notifications

Do NOT modify files until this gap analysis is complete.

============================================================
PHASE 4 — PRESERVE THE PROJECT ARCHITECTURE
============================================================

The intended package structure should remain approximately:

include/
└── remarketing/
    ├── __init__.py
    ├── audit.py
    ├── calculations.py
    ├── config_loader.py
    ├── constants.py
    ├── data_validation.py
    ├── excel_reader.py
    ├── exceptions.py
    ├── gcs_utils.py
    ├── mappings.py
    ├── monthly_reporting.py
    ├── notifications.py
    ├── pipeline.py
    ├── pivot_reporting.py
    ├── reconciliation.py
    ├── report_builder.py
    ├── standardization.py
    ├── workbook_validation.py
    └── ytd_reporting.py

Do not create one giant Python file.

Responsibilities must remain separated.

============================================================
PHASE 5 — BUSINESS LOGIC IMPLEMENTATION
============================================================

Implement ONLY business rules supported by WORKBOOK_LOGIC_SPEC.md.

For every implemented rule add a comment containing its specification ID.

Example:

# SPEC: FORMULA-017
# Excel equivalent: ...
# Business output: ...

Do not paste proprietary source data into comments.

For each rule ensure:

- input fields match specification
- calculation matches Excel semantics
- null handling matches Excel
- date handling matches Excel
- rounding matches Excel
- text comparison matches Excel
- lookup semantics match Excel
- fallback behavior matches Excel
- output column matches expected French label

============================================================
PHASE 6 — EXCEL SEMANTICS
============================================================

Pay careful attention to differences between Excel and Python.

Explicitly account for:

- blank cells
- empty strings
- NaN
- pd.NA
- NaT
- zero
- False
- Excel serial dates
- text dates
- numbers stored as text
- leading/trailing spaces
- non-breaking spaces
- accented French characters
- case differences
- Excel rounding
- percentage calculations
- IFERROR behavior
- VLOOKUP/XLOOKUP missing matches
- COUNT vs COUNTA
- SUM ignoring blanks
- date boundaries
- month-end behavior

Do not translate an Excel formula mechanically if pandas would behave differently.

The Python result must mimic Excel behavior.

============================================================
PHASE 7 — CALCULATIONS.PY
============================================================

Review every existing function.

Current project may contain functions such as:

as_of_date()
add_date_parts()
inventory_status()
inventory_age_days()
age_bucket()
maturity_months()
net_of_vat()
capped_collection_fee()
monthly_amount()
residual_floor()
mileage_charge()
apply_rules()

For EACH function determine:

1. Is it actually supported by WORKBOOK_LOGIC_SPEC.md?
2. Does its implementation match the confirmed Excel formula?
3. Does the formula belong in this function?
4. Is it used?
5. Is it provisional?
6. Should it be removed?
7. Should it remain but be marked unconfirmed?

Do not keep fabricated formulas merely for API compatibility.

If a helper is not supported by workbook evidence and is unused, remove it.

If it must remain temporarily for compatibility:

- clearly document that it is not part of confirmed business behavior
- do not invoke it from apply_rules()

============================================================
PHASE 8 — APPLY_RULES()
============================================================

apply_rules() should become the deterministic entry point for confirmed row-level calculations.

It should:

1. validate required inputs
2. calculate rules in dependency order
3. preserve original source fields
4. add derived fields
5. not silently overwrite unrelated source data
6. not make external calls
7. be idempotent
8. generate the same result when rerun on the same input

Rule ordering must come from the formula dependency graph.

Example:

source fields
    ↓
derived A
    ↓
derived B
    ↓
derived C

Do not calculate C before A/B if the workbook does not.

============================================================
PHASE 9 — LOOKUPS AND MAPPINGS
============================================================

Use WORKBOOK_LOGIC_SPEC.md section:

Lookup and Mapping Rules

Implement every confirmed lookup.

Preserve Excel match semantics:

- exact match
- approximate match
- fallback values
- missing-match handling
- normalization requirements
- duplicate lookup-key behavior

Do not convert all lookups to a generic LEFT JOIN unless that matches Excel behavior.

Where feasible create reusable lookup functions.

============================================================
PHASE 10 — MONTHLY REPORTING
============================================================

Rewrite/review:

include/remarketing/monthly_reporting.py

using the confirmed Monthly Reporting Logic section.

Do NOT assume:

Date de retour

is the reporting date unless the spec confirms it for that metric.

Different metrics may use different reporting dates.

Implement the monthly rule matrix exactly.

Tests must cover:

- first day of month
- last day of month
- records outside period
- blank dates
- different years
- period boundary

============================================================
PHASE 11 — YTD REPORTING
============================================================

Rewrite/review:

include/remarketing/ytd_reporting.py

using the confirmed YTD specification.

Pay special attention to historical corrections.

If the confirmed process requires:

February correction
    ↓
rebuild February
    ↓
rebuild subsequent YTD periods

implement it explicitly.

If the behavior remains unconfirmed:

DO NOT invent it.

Expose a clear configuration/error/warning and retain the question in OPEN_QUESTIONS.md.

============================================================
PHASE 12 — PIVOT REPORTING
============================================================

Use the ACTUAL PivotTable definitions from WORKBOOK_LOGIC_SPEC.md.

Review:

include/remarketing/pivot_reporting.py

For each confirmed PivotTable implement equivalent aggregation logic.

Each pivot implementation must specify:

- source dataset
- rows
- columns
- values
- aggregation
- filters
- grouping
- sorting where relevant

Do NOT approximate Excel PivotTables based on sheet names.

============================================================
PHASE 13 — REPORT BUILDER
============================================================

Review:

include/remarketing/report_builder.py

Generate an XLSX output representing the required business reports.

Preserve French labels.

Where confirmed, recreate:

- detail sheets
- summary sheets
- monthly reports
- YTD reports
- pivot-equivalent reports
- KPI reports
- Remarketing report
- ERA report
- other confirmed outputs

Do not fabricate missing layouts.

If exact formatting is not available from metadata:

focus on DATA/LOGIC parity first.

Document formatting differences separately.

============================================================
PHASE 14 — RECONCILIATION
============================================================

Reconciliation is mandatory.

Review:

include/remarketing/reconciliation.py

Implement reconciliation checks from WORKBOOK_LOGIC_SPEC.md.

Include, where supported:

- input row count
- valid row count
- excluded row count
- contract count
- status totals
- returned vehicle count
- inventory count
- aging totals
- monthly totals
- YTD totals
- pivot totals
- financial totals

Use deterministic tolerances.

For exact integer/count metrics:

expected difference = 0

For financial floating-point metrics use only a justified tolerance.

Never silently pass a mismatch.

============================================================
PHASE 15 — CONFIGURATION
============================================================

Review all:

config/*.yaml

Move genuine configuration out of code where appropriate.

Examples:

- source worksheet
- expected source columns
- reporting settings
- confirmed bucket boundaries
- configured mappings
- output sheet names

Do NOT move fundamental formula logic to YAML merely to avoid implementing it correctly.

Update:

business_rules.yaml
column_mapping.yaml
report_config.yaml
workbook_config.yaml

based on the specification.

============================================================
PHASE 16 — SCHEMA HANDLING
============================================================

Continue using robust column normalization.

Handle:

- Unicode normalization
- NBSP
- zero-width characters
- leading/trailing whitespace
- repeated whitespace

But:

DO NOT fuzzy-map business columns automatically.

Example:

"Contrat"

must not automatically map to:

"Contrat Terms (mois)"

Similarity does not prove semantic equivalence.

Ambiguous mapping must fail explicitly.

============================================================
PHASE 17 — TEST-DRIVEN IMPLEMENTATION
============================================================

For every confirmed business rule:

1. create a failing synthetic test
2. run it
3. verify the failure represents missing/incorrect behavior
4. implement the rule
5. rerun the test
6. make it pass
7. run regression tests

Use synthetic data only.

Test naming:

test_formula_001_...
test_monthly_rule_...
test_ytd_rule_...
test_pivot_...
test_lookup_...

Where possible reference the spec rule ID in the test.

============================================================
PHASE 18 — TEST CATEGORIES
============================================================

The finished project should contain:

UNIT TESTS
-----------
formula calculations
lookup behavior
null handling
date handling
standardization
aging boundaries

INTEGRATION TESTS
-----------------
source DataFrame
    ↓
standardization
    ↓
calculations
    ↓
monthly/YTD
    ↓
report data

SCHEMA TESTS
------------
expected workbook
missing sheet
missing column
ambiguous normalized column

RECONCILIATION TESTS
--------------------
matching totals
intentional mismatch detection

IDEMPOTENCY TESTS
-----------------
same input twice
=
same output data

HISTORICAL REBUILD TESTS
------------------------
only where confirmed by specification

============================================================
PHASE 19 — RUN_LOCAL.PY
============================================================

Review:

scripts/run_local.py

It must execute the same core business code used by Airflow.

Do not create a separate local implementation of calculations.

Expected conceptual flow:

load config
    ↓
locate input
    ↓
validate workbook
    ↓
read source sheets
    ↓
validate source schema
    ↓
standardize
    ↓
lookups
    ↓
apply rules
    ↓
monthly
    ↓
YTD
    ↓
pivot/report datasets
    ↓
reconciliation
    ↓
generate output

Make error messages precise.

============================================================
PHASE 20 — AIRFLOW DAGS
============================================================

Review:

dags/remarketing_file_watcher_dag.py
dags/remarketing_processing_dag.py

Airflow must orchestrate.

DO NOT duplicate business formulas inside DAG files.

DAG tasks should call functions from:

include/remarketing/

The processing DAG should conceptually perform:

validate
    ↓
extract
    ↓
standardize
    ↓
map
    ↓
calculate
    ↓
monthly
    ↓
YTD
    ↓
report
    ↓
reconcile
    ↓
publish/archive
    ↓
notify

Ensure retries do not duplicate outputs.

============================================================
PHASE 21 — GCS
============================================================

Review:

include/remarketing/gcs_utils.py

Implement generic GCS behavior only.

Expected concepts:

incoming
raw/archive
processing
reports
invalid
audit

Input files should remain recoverable for audit/reprocessing.

Do not hard-code confidential bucket names unless they already exist in approved configuration.

Use environment variables/Airflow configuration.

============================================================
PHASE 22 — FILE ARRIVAL
============================================================

The desired workflow is event-oriented.

If the current watcher DAG polls GCS:

clearly document:

POLLING FALLBACK

Do not call polling "true event-driven processing".

If the target environment already provides a supported GCS event mechanism, structure the processing DAG so it can be triggered with object metadata without changing business logic.

============================================================
PHASE 23 — AUDITABILITY
============================================================

Every pipeline run should have enough metadata to identify:

- source object/file
- source checksum if feasible
- reporting period
- execution/run ID
- processing timestamp
- row counts
- report output location
- reconciliation status

Do not log sensitive row-level data.

============================================================
PHASE 24 — DOCUMENTATION UPDATE
============================================================

Update:

README.md
BUSINESS_RULES.md
OPEN_QUESTIONS.md

Create if useful:

IMPLEMENTATION_TRACEABILITY.md

Traceability format:

| Spec Rule | Python Module | Function | Test | Status |

Example statuses:

IMPLEMENTED_CONFIRMED
IMPLEMENTED_HIGH_CONFIDENCE
BLOCKED_ANALYST_VALIDATION
NOT_REQUIRED

============================================================
PHASE 25 — OPEN QUESTIONS
============================================================

OPEN_QUESTIONS.md must contain ONLY genuinely unresolved items after implementation.

For each:

QUESTION ID
Specification section
Current evidence
Why implementation depends on it
Affected module
Temporary behavior

Never silently close a question by choosing an arbitrary interpretation.

============================================================
PHASE 26 — REMOVE FALSE ASSUMPTIONS
============================================================

Explicitly search the current ZIP for provisional assumptions.

Examples might include:

hard-coded:
    Solde
    Non Solde
    VAT 20%
    collection fee 8%
    cap 1200
    mileage charge 0.005
    inventory aging formula
    aging bucket limits
    specific reporting date

For EACH one:

verify against WORKBOOK_LOGIC_SPEC.md.

If not confirmed:

do not allow it to influence final business output.

Remove, disable, or mark as pending.

============================================================
PHASE 27 — STATIC VERIFICATION
============================================================

Run:

python compile checks

Example:

python -m compileall include scripts dags

There must be no syntax errors.

============================================================
PHASE 28 — TEST VERIFICATION
============================================================

Run the COMPLETE test suite.

Example:

pytest -v

Report:

tests collected
tests passed
tests failed
tests skipped

Do NOT say the project is complete if tests are failing.

============================================================
PHASE 29 — LOCAL SYNTHETIC END-TO-END TEST
============================================================

Create a fabricated workbook matching the required source schema.

Do NOT use real client records.

Run:

python -m scripts.audit_schema

and:

python -m scripts.run_local

against the synthetic workbook.

Verify that a report is produced.

============================================================
PHASE 30 — REAL WORKBOOK VALIDATION
============================================================

If the actual source workbook exists inside this approved system, run the project against a COPY of it.

DO NOT overwrite the original workbook.

Use an explicit reporting period.

Example:

TEST_PERIOD=YYYY-MM

Record:

- run status
- runtime
- rows processed
- source validation
- generated output
- reconciliation results

Do not reproduce row-level confidential data in logs.

============================================================
PHASE 31 — MANUAL VS AUTOMATED COMPARISON
============================================================

If a manually approved Excel result is available inside this environment:

compare:

MANUAL
vs
AUTOMATED

for each reconciliation metric.

Generate:

RECONCILIATION_RESULT.md

Example structure:

| Metric | Manual | Automated | Difference | Result |

Result:

PASS
FAIL
NOT_AVAILABLE

Do not modify Python logic merely to force a PASS.

Trace failures back to the relevant workbook rule.

============================================================
PHASE 32 — IMPLEMENTATION READINESS
============================================================

At completion provide:

Confirmed spec rules implemented:
Confirmed spec rules tested:
High-confidence rules implemented:
Rules blocked by analyst questions:
Pivot definitions implemented:
Monthly rules implemented:
YTD rules implemented:
Reconciliation checks implemented:
Tests passed:
Tests failed:

Then classify:

TECHNICALLY_READY_FOR_UAT

or

PARTIALLY_READY_FOR_UAT

or

NOT_READY_FOR_UAT

Do not use:

PRODUCTION READY

unless manual reconciliation and required business approval have occurred.

============================================================
FINAL DELIVERABLES
============================================================

Produce:

1. UPDATED PROJECT FOLDER

2. UPDATED ZIP

Name:

remarketing_airflow_automation_spec_aligned.zip

3. IMPLEMENTATION_GAP_ANALYSIS.md

4. IMPLEMENTATION_TRACEABILITY.md

5. UPDATED BUSINESS_RULES.md

6. UPDATED OPEN_QUESTIONS.md

7. TEST_RESULTS.md

8. RECONCILIATION_RESULT.md
   if a manual benchmark exists

9. CHANGE_SUMMARY.md

============================================================
CHANGE_SUMMARY FORMAT
============================================================

CHANGE_SUMMARY.md must include:

# Change Summary

## Business Rules Replaced

For each provisional rule removed/replaced:

Current behavior:
New behavior:
Specification evidence:
Rule ID:
Files changed:

## New Business Rules Added

Rule:
Spec ID:
Implementation:
Tests:

## Rules Not Implemented

Rule:
Reason:
Required analyst answer:

## Pivot Changes

## Monthly Reporting Changes

## YTD Reporting Changes

## Reconciliation Changes

## Airflow Changes

## Config Changes

## Removed Assumptions

## Test Summary

============================================================
CODE QUALITY REQUIREMENTS
============================================================

Use:

- Python type hints
- small focused functions
- deterministic calculations
- explicit error messages
- configuration where appropriate
- docstrings for non-obvious logic
- no hidden global state
- no business logic inside Airflow DAG declarations
- no duplicated calculation logic
- no secrets in source code
- no hard-coded local machine paths
- no customer data in tests

Avoid unnecessary abstractions.

============================================================
MOST IMPORTANT IMPLEMENTATION RULE
============================================================

Do not rewrite the Excel business process into what you think is a better process.

FIRST reproduce the CURRENT process.

Existing Excel behavior
        ↓
Python equivalent
        ↓
reconciliation
        ↓
business approval

Only after parity has been achieved should optimization or redesign be considered.

============================================================
FINAL INSTRUCTION
============================================================

Start by doing ALL of the following before editing code:

1. extract and inventory the ZIP
2. read the entire WORKBOOK_LOGIC_SPEC.md
3. build the rule-to-code gap matrix
4. identify every provisional assumption in the current code
5. identify every unresolved specification rule

SHOW ME THAT ANALYSIS FIRST.

DO NOT MODIFY ANY PROJECT FILE UNTIL I APPROVE THE GAP ANALYSIS.

After I approve it, proceed with implementation using test-driven changes.

When implementation is complete:

- run all tests
- run compile validation
- run local synthetic end-to-end validation
- run the real workbook locally if it is available inside this approved environment
- produce the final updated ZIP

Never claim a business rule is confirmed unless WORKBOOK_LOGIC_SPEC.md supports it.
