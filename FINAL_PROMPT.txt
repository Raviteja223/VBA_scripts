Approved to proceed with CORE IMPLEMENTATION ONLY.

Use the updated Workbook Logic Specification, Evidence Gap Report,
Pivot Specification, External Dependencies, Analyst Questions,
and Implementation Readiness V2 as the source of truth.

Implement ONLY rules classified as sufficiently supported for core implementation.

Do NOT implement or guess:

- formulas classified as SOURCE_WORKBOOK_DEFECT
- unresolved #REF! logic
- monthly report rules lacking explicit date evidence
- YTD or historical rebuild rules
- unavailable external-workbook-dependent logic
- Pivot filter/hidden-item behavior not conclusively recovered
- business interpretations of technically different aging fields
- full Excel report reproduction

For every implemented business rule, require traceability:

Spec Rule ID
→ Python module
→ function
→ synthetic test
→ implementation status

Use these implementation statuses:

IMPLEMENTED_CONFIRMED
IMPLEMENTED_TECHNICALLY_PROVEN
BLOCKED_SOURCE_WORKBOOK_DEFECT
BLOCKED_EXTERNAL_DEPENDENCY
BLOCKED_ANALYST_VALIDATION
NOT_IMPLEMENTED

Before changing code, produce the final core-only gap analysis against:

remarketing_airflow_automation_final.zip

Then STOP and show me the gap analysis for approval.

Do not modify project files until I approve that gap analysis.

The objective of this phase is:

CONFIRMED CORE CALCULATION ENGINE

not:

FULL EXCEL REPORT REPRODUCTION.
