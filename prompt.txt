Act as a Senior Data Engineer / Airflow Architect with strong experience in:

- Apache Airflow
- Astronomer
- Google Cloud Storage
- Python
- pandas
- openpyxl / XlsxWriter
- Excel formula reverse engineering
- Excel PivotTables
- production-grade ETL/automation
- idempotent batch processing
- business-rule migration from Excel to Python

I am uploading 4 XLSX files generated by analyzing a very large production Excel workbook.

These are NOT the original raw business-data workbook.

They are metadata/extraction outputs generated from the original workbook:

1. WORKBOOK_STRUCTURE.xlsx
   Contains information such as:
   - worksheet names
   - visible / hidden / very hidden sheets
   - row counts
   - column counts
   - formula counts
   - PivotTable counts
   - Excel table counts
   - used ranges

2. FORMULA_PATTERNS.xlsx
   Contains normalized unique Excel formula patterns extracted using FormulaR1C1 or equivalent.
   It may contain:
   - sheet
   - column
   - example cell
   - normalized formula
   - example original formula
   - formula type
   - cross-sheet reference indicator
   - occurrence count

3. NAMED_RANGES.xlsx
   Contains:
   - workbook/sheet named ranges
   - scope
   - RefersTo
   - visibility
   - cross-sheet references
   - possible external references

4. PIVOT_CATALOG.xlsx
   Contains PivotTable metadata such as:
   - worksheet
   - pivot name
   - pivot location
   - source
   - fields
   - row fields
   - column fields
   - filters
   - value/data fields
   - aggregation type
   - field position

==================================================
PROJECT CONTEXT
==================================================

The client currently has a manual Excel-based remarketing / vehicle inventory reporting process.

The business user receives several Excel files and manually performs calculations and creates reports every month.

Known business requirements include:

- returned vehicles
- vehicles currently in inventory
- inventory/vehicle aging
- monthly calculations
- year-to-date calculations
- historical corrections
- seller/channel mappings
- remarketing report
- ERA report
- PivotTable/report outputs

The user currently performs calculations manually in Excel.

The objective is to automate the Excel workflow.

Current target architecture:

Users upload Excel files to GCS
        ↓
Airflow / Astronomer
        ↓
validate input
        ↓
clean / standardize
        ↓
apply mappings
        ↓
perform the same calculations currently performed in Excel
        ↓
generate monthly/YTD reports
        ↓
generate final XLSX output
        ↓
upload output to GCS
        ↓
email/notification
        ↓
archive/audit

IMPORTANT:

This project DOES NOT currently require BigQuery.

The existing discussion concluded that the required source data is already provided as Excel files and the workflow primarily needs to manipulate Excel data and create Excel reports.

Do NOT introduce BigQuery, dbt, Spark, Dataflow or other unnecessary technology unless the uploaded workbook metadata proves there is a genuine requirement.

==================================================
CURRENT AIRFLOW DESIGN
==================================================

We expect approximately TWO DAGs.

--------------------------------------------------
DAG 1 — GCS FILE WATCHER / ARRIVAL DAG
--------------------------------------------------

Purpose:

Detect incoming input files and trigger the main processing DAG.

Responsibilities should include:

- detect new Excel file in GCS
- identify exact object name
- identify bucket
- identify GCS object generation/version if useful
- detect/reporting period from filename or metadata where possible
- validate filename
- validate extension
- preserve/copy original input
- prevent accidental duplicate processing
- perform lightweight pre-checks
- trigger DAG 2
- pass file metadata to DAG 2
- error handling
- notification on watcher/pre-check failure

The intended business requirement is event-driven processing:

Whenever a new eligible file is uploaded, processing should begin.

If true GCS-event-to-Airflow triggering requires infrastructure outside the DAG itself, clearly separate:

A. DAG code
B. external trigger/infrastructure requirement

Do not pretend cron polling is truly event-driven.

If a polling watcher is required as a fallback, isolate it clearly and explain why.

--------------------------------------------------
DAG 2 — MAIN REMARKETING PROCESSING DAG
--------------------------------------------------

The existing skeleton concept is:

extract
    ↓
validate
    ↓
standardize
    ↓
calculate
    ↓
output/report
    ↓
notify

Convert this into a proper production workflow based on the uploaded workbook analysis.

Expected conceptual tasks may include:

extract_file
    ↓
validate_workbook
    ↓
validate_required_fields
    ↓
clean_source_data
    ↓
standardize_values
    ↓
apply_reference_mappings
    ↓
calculate_vehicle_metrics
    ↓
calculate_monthly_metrics
    ↓
calculate_ytd_metrics
    ↓
generate_remarketing_report
    ↓
generate_era_report
    ↓
validate_generated_reports
    ↓
upload_reports_to_gcs
    ↓
write_audit_record
    ↓
notify_success

The actual task breakdown must be determined from the uploaded workbook metadata.

==================================================
CRITICAL REQUIREMENT:
REVERSE ENGINEER THE EXCEL LOGIC FIRST
==================================================

DO NOT immediately generate DAG code.

FIRST deeply analyze ALL FOUR uploaded XLSX files together.

Your first task is to reconstruct as much of the original Excel business logic as possible.

Build a complete model of:

Workbook
    ↓
Source/Input sheets
    ↓
Lookup/mapping sheets
    ↓
Intermediate/calculation sheets
    ↓
Formula dependencies
    ↓
Pivot/report sheets
    ↓
Final reports

Identify:

- source sheets
- intermediate sheets
- report/output sheets
- hidden calculation sheets
- mapping/reference sheets
- repeated formula patterns
- cross-sheet dependencies
- named-range dependencies
- PivotTable dependencies
- external workbook dependencies if visible
- historical/monthly relationships

==================================================
FORMULA ANALYSIS
==================================================

Deeply analyze every unique formula pattern.

Do NOT merely list Excel formulas.

Translate every meaningful pattern into a business/technical rule.

For each discovered rule produce:

RULE ID

Example:

BR-001

Rule name:
Inventory Age

Source Sheet:
...

Source Columns:
...

Formula:
...

Normalized Formula:
...

Referenced Sheets:
...

Business interpretation:
...

Python equivalent:
...

Used By:
Monthly report / YTD / pivot / other

Confidence:
HIGH / MEDIUM / LOW

Reason:
...

Examples of formula categories to interpret:

- IF
- IFS
- SUM
- SUMIF
- SUMIFS
- COUNTIF
- COUNTIFS
- AVERAGEIF
- AVERAGEIFS
- VLOOKUP
- XLOOKUP
- INDEX/MATCH
- date calculations
- string normalization
- cross-sheet references
- SUBTOTAL
- GETPIVOTDATA
- arithmetic calculations

If formulas reference relative cells in R1C1 notation, convert them back into understandable column/business dependencies.

==================================================
PIVOT ANALYSIS
==================================================

Reconstruct every PivotTable logically.

For each pivot identify:

- source
- row fields
- column fields
- filters
- value fields
- aggregation:
  - count
  - sum
  - average
  - min
  - max
  - distinct count if identifiable
- sorting
- report purpose where inferable

Then design the equivalent Python/pandas calculation.

Example:

Excel Pivot:

Rows:
Seller

Columns:
Age Bucket

Values:
Count of VIN

Filter:
Status = Inventory

Equivalent:

pd.pivot_table(...)

Do NOT rely on Excel PivotTables during production processing if equivalent deterministic pandas calculations can produce the same report.

However, preserve the final required workbook layout where practical.

==================================================
NAMED RANGE ANALYSIS
==================================================

Analyze named ranges for:

- mappings
- lookup tables
- report ranges
- parameter values
- dates
- thresholds
- external dependencies

Use them to help infer business rules.

==================================================
HISTORICAL / YTD REQUIREMENT
==================================================

A particularly important business requirement is historical correction.

Example:

A February vehicle may later change or be returned.

The user currently has to:

update February
    ↓
recalculate/rebuild subsequent cumulative reports
    ↓
January → current month YTD changes

Design the processing architecture to handle this correctly.

Determine from the workbook metadata whether:

- each monthly file contains only that month's data
- each file contains YTD data
- historical months are independently stored
- monthly reports reference prior monthly workbooks
- YTD reports are recomputed from historical periods

If the metadata cannot determine this confidently, do NOT silently invent behavior.

Create a clearly isolated configuration or function for historical rebuild strategy.

The pipeline must be idempotent.

Processing the same exact input twice must not double-count business data.

==================================================
FRENCH BUSINESS LABELS
==================================================

The original workbook uses French column/report names.

Final reports must preserve the business-facing French names.

Internally, Python may use normalized English identifiers if that makes the code maintainable.

For example:

French column
      ↓
internal canonical name
      ↓
business calculation
      ↓
French output label restored

Do not permanently translate or change the client's final report labels.

==================================================
PROJECT STRUCTURE
==================================================

Do NOT put the complete business logic directly inside DAG files.

Create a clean project structure approximately like:

dags/
    remarketing_file_watcher_dag.py
    remarketing_processing_dag.py

include/
    remarketing/
        __init__.py
        constants.py
        config_loader.py
        gcs_utils.py
        excel_reader.py
        workbook_validation.py
        data_validation.py
        standardization.py
        mappings.py
        calculations.py
        monthly_reporting.py
        ytd_reporting.py
        pivot_reporting.py
        report_builder.py
        reconciliation.py
        audit.py
        notifications.py

config/
    workbook_config.yaml
    column_mapping.yaml
    business_rules.yaml
    report_config.yaml

tests/
    test_validation.py
    test_standardization.py
    test_calculations.py
    test_monthly_reporting.py
    test_ytd_reporting.py

requirements.txt
README.md

Adjust this structure based on what is actually found in the four XLSX files.

==================================================
AIRFLOW CODING REQUIREMENTS
==================================================

Use production-quality Apache Airflow code suitable for Astronomer.

Prefer TaskFlow API where reasonable.

For example:

@dag(...)
def remarketing_processing():

    @task
    def validate_workbook(...):
        ...

But use provider operators/hooks when they are more appropriate.

Use proper:

- retries
- retry delays
- execution timeouts
- failure handling
- logging
- task dependencies
- trigger rules where required
- parameters
- XCom only for SMALL metadata
- GCS paths for actual files
- task grouping if it improves readability

DO NOT pass pandas DataFrames or Excel file contents through XCom.

Pass only metadata such as:

bucket
object_name
generation
run_id
reporting_period
temporary GCS path
output path

Large data should remain in:

- GCS
- local ephemeral task storage where appropriate

==================================================
GCS DESIGN
==================================================

Use a clean structure such as:

incoming/
raw/
processing/
reports/
invalid/
archive/
audit/

For example:

incoming/
    remarketing_2026_09.xlsx

raw/
    2026/
        09/
            <run_id>/
                original.xlsx

reports/
    2026/
        09/
            remarketing_report_2026_09.xlsx

invalid/
archive/
audit/

Do not overwrite the only copy of the original input.

==================================================
VALIDATION
==================================================

Build validation based on what is visible in the workbook metadata.

Possible checks include:

- valid XLSX
- expected workbook structure
- required sheets
- required columns
- mandatory fields
- date parsing
- numeric parsing
- duplicate business keys
- unexpected lookup values
- expected reporting period
- empty input
- invalid status values

If critical validation fails:

STOP the processing.

Record the reason.

Send/trigger error notification.

Do not generate a misleading report.

==================================================
CONFIGURATION VS CODE
==================================================

Do not hardcode changeable business mappings inside Python unless necessary.

Things such as:

- seller → channel mapping
- aging thresholds
- status mappings
- report sheet names
- required fields
- filename patterns

should preferably live in YAML/configuration where appropriate.

Example:

seller_channel_mapping:
  "<French seller value>": "<Channel>"

However:

If the XLSX files indicate that a business rule is inherently formula/code logic rather than configuration, implement it as code.

==================================================
REPORT GENERATION
==================================================

Generate XLSX output using pandas + openpyxl or XlsxWriter.

Preserve:

- required sheet names
- French column labels
- report layout where identifiable
- calculated summaries
- monthly metrics
- YTD metrics
- Pivot-equivalent summaries

Where possible also preserve:

- number formats
- dates
- totals
- headers
- column ordering

Do not attempt to reproduce decorative formatting that does not affect business use unless the uploaded files indicate it is required.

==================================================
RECONCILIATION
==================================================

Create explicit validation/reconciliation before publishing.

Possible checks:

source_count
valid_count
excluded_count
returned_count
inventory_count
monthly totals
YTD totals
pivot totals
report totals

Ensure important totals reconcile where mathematically appropriate.

A successful Airflow DAG is NOT sufficient.

The generated business data must also be validated.

==================================================
IMPORTANT: DO NOT INVENT UNKNOWN BUSINESS RULES
==================================================

The four XLSX metadata files may not expose every business decision.

If something cannot be reliably determined:

DO NOT MAKE UP THE RULE.

Instead:

1. isolate the uncertain part;
2. implement a configurable placeholder where possible;
3. document it in:

OPEN_QUESTIONS.md

Use:

QUESTION
Evidence
Why it matters
What needs confirmation
What code is affected

Example:

QUESTION:
Which date should be used for inventory aging?

Evidence:
Workbook formula refers to X, but named range suggests Y.

Needs confirmation:
Analyst needs to confirm whether aging is based on current date or month-end date.

The rest of the implementation should still be completed wherever the uploaded evidence is sufficient.

==================================================
ANALYSIS OUTPUT BEFORE CODE
==================================================

Before creating code, show me:

1. WORKBOOK ARCHITECTURE

2. SHEET CLASSIFICATION
   - input
   - mapping
   - intermediate
   - output/report
   - unknown

3. FORMULA BUSINESS RULE CATALOG

4. PIVOT TABLE CATALOG

5. CROSS-SHEET LINEAGE

6. EXTERNAL DEPENDENCIES

7. CALCULATIONS THAT CAN BE FULLY AUTOMATED

8. CALCULATIONS THAT ARE AMBIGUOUS

9. PROPOSED AIRFLOW TASK GRAPH

10. PROPOSED PROJECT STRUCTURE

Then proceed to implementation.

==================================================
FINAL DELIVERABLES
==================================================

After analysis, generate the complete project.

At minimum provide:

1.
dags/remarketing_file_watcher_dag.py

2.
dags/remarketing_processing_dag.py

3.
All required Python modules under:

include/remarketing/

4.
Required YAML configuration files.

5.
requirements.txt

6.
README.md

7.
OPEN_QUESTIONS.md

8.
BUSINESS_RULES.md

9.
Tests for the important calculations that can be inferred from the uploaded metadata.

10.
A folder/file tree.

==================================================
DAG 1 EXPECTATION
==================================================

The watcher DAG should:

- identify the incoming GCS object
- validate basic metadata
- copy/archive raw input if needed
- determine reporting context
- trigger processing DAG
- pass only metadata
- prevent accidental duplicate submissions where practical
- log clearly
- fail clearly

==================================================
DAG 2 EXPECTATION
==================================================

Main processing DAG should approximately implement:

START
  ↓
initialize_run
  ↓
copy_or_resolve_input
  ↓
validate_workbook
  ↓
extract_excel_data
  ↓
validate_business_data
  ↓
standardize
  ↓
apply_mappings
  ↓
calculate_business_metrics
  ↓
calculate_monthly
  ↓
calculate_ytd
  ↓
build_remarketing_report
  ↓
build_era_report
  ↓
reconcile
  ↓
upload_output
  ↓
audit
  ↓
notify
  ↓
END

Modify the graph if the workbook analysis proves another design is more appropriate.

==================================================
QUALITY EXPECTATIONS
==================================================

I want production-quality code.

Do not give pseudocode.

Do not leave generic comments like:

# implement calculation here

if the calculation can be derived from the uploaded files.

Do not create one giant Python file.

Use:

- functions
- modules
- dataclasses where useful
- type hints
- structured logging
- defensive validation
- clear exception classes
- configuration
- unit-testable transformation functions

Avoid:

- unnecessary abstraction
- unnecessary cloud services
- huge DAG files
- hidden side effects
- hardcoded environment-specific credentials

Use Airflow Connections / environment configuration for cloud authentication.

==================================================
FINAL CODE REVIEW
==================================================

Before giving me the final output, perform an internal review for:

- incorrect Airflow dependencies
- deprecated Airflow usage
- Astronomer compatibility
- GCS path mistakes
- XCom misuse
- non-idempotent logic
- duplicate processing risk
- incorrect Excel formula translation
- incorrect Pivot aggregation
- cross-sheet dependency mistakes
- historical/YTD recalculation errors
- hardcoded secrets
- unhandled missing columns
- unhandled null values
- large-memory risks
- accidental source-file overwrite

Then provide the final project.

If your environment supports generating downloadable files, package everything as:

remarketing_airflow_automation.zip

Do not stop after explaining the solution.

Generate the actual final source files.
